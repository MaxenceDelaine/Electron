{
  "name": "electron-quick-start",
  "version": "1.0.0",
  "description": "A minimal Electron application",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "build:osx": "electron-packager . --out=dist --overwrite --platform=darwin --arch=x64",
    "build:win64": "electron-packager . --out=dist --overwrite --platform=win32 --arch=x64",
    "build:arm": "electron-packager . --out=dist --overwrite --platform=linux --arch=armv7l",
    "build:linux": "electron-packager . --out=dist --overwrite --platform=linux --arch=x64",
    "build": "electron-packager . --out=dist --asar --overwrite --all"
  },
  "repository": "https://github.com/electron/electron-quick-start",
  "keywords": [
    "Electron",
    "quick",
    "start",
    "tutorial",
    "demo"
  ],
  "author": "GitHub",
  "license": "CC0-1.0",
  "devDependencies": {
    "devtron": "^1.4.0",
    "electron": "^1.8.4",
    "electron-builder": "^20.13.3"
  },
  "dependencies": {
    "bootstrap": "^4.1.1",
    "electron-packager": "^12.0.2",
    "jquery": "^3.3.1",
    "photon": "^2.0.1"
  }
}
